# 昵称过长三层防护说明

> 目的：防止客户设置超长昵称（英文串、中文长词、emoji 拼接等）导致导航栏、authBadge、账户页破版或挤压其他元素，同时保证完整信息可被查看。
>
> 总体思路：写入限宽 → 容器裁断 → 可读兜底，三层互相备份，任意一层失效都不会出现 UI 异常。

---

## 第 1 层：写入层硬截断（根本上禁止超长脏数据）

**意图**：在 `user_metadata.name` 入库前就把长度卡死，避免超长数据长期存在。

- **阈值**：最大 20 字符（中英文、emoji、数字等统一按 JS string.length 计算，电商场景足够使用；若需兼容 4 字节 emoji，可再升级为 Array.from().length）
- **HTML 前端约束**：`account.html` 里 `fName` 输入框 `maxlength="20"`，并在 hint 明示上限
- **JS 后端兜底**：即使通过 DevTools 手动去掉 maxlength，`saveProfile()` 在提交前仍会 `.slice(0,20)`

### 实现点
- [account.html - fName 输入框 + hint](file:///Users/micahma/Library/Application%20Support/TRAE%20SOLO%20CN/ModularData/ai-agent/work-mode-projects/6a7a8fd2f5e3f47aa813dd7d/13am-site/account.html#L1099-L1103)
- [account.html - saveProfile 截断逻辑](file:///Users/micahma/Library/Application%20Support/TRAE%20SOLO%20CN/ModularData/ai-agent/work-mode-projects/6a7a8fd2f5e3f47aa813dd7d/13am-site/account.html#L1355-L1364)

```javascript
// 关键代码
const newName = (fName.value || '').trim().slice(0,20); // 硬上限 20 字符
```

---

## 第 2 层：CSS ellipsis 容器裁断（宽度被挤压也不跑版）

**意图**：若以后调大 20 字符上限、或页面整体变窄（移动端、侧边栏挤压等），UI 容器依然保持形状，不撑开、不换行、不遮挡其他元素。

### 通用策略
```css
white-space: nowrap;      /* 不换行，必须单行 */
overflow: hidden;         /* 超出容器的部分裁掉 */
text-overflow: ellipsis;  /* 裁断末尾显示 ... */
max-width: <按场景定制>;  /* 关键：给文字一个明确的最大宽度 */
vertical-align: middle;
```

### 各页面实现点

#### 首页导航栏 `.user-name-label`
- `max-width: 112px`（13px 字体、字母间距 0.1em 的条件下，约 10~12 个拉丁字母或 6~8 个汉字）
- [index.html - CSS 定义](file:///Users/micahma/Library/Application%20Support/TRAE%20SOLO%20CN/ModularData/ai-agent/work-mode-projects/6a7a8fd2f5e3f47aa813dd7d/13am-site/index.html#L907-L915)

#### 购物车 / 商品详情页 `.ab-text`
- `max-width: 120px`（略宽于导航栏，因为右上角固定区域本身更宽裕）
- [cart.html - CSS 定义](file:///Users/micahma/Library/Application%20Support/TRAE%20SOLO%20CN/ModularData/ai-agent/work-mode-projects/6a7a8fd2f5e3f47aa813dd7d/13am-site/cart.html#L464-L472)
- [product.html - CSS 定义](file:///Users/micahma/Library/Application%20Support/TRAE%20SOLO%20CN/ModularData/ai-agent/work-mode-projects/6a7a8fd2f5e3f47aa813dd7d/13am-site/product.html#L693-L701)

---

## 第 3 层：JS 软截断 + 原生 tooltip（视觉简洁 + 完整可读）

**意图**：即使 20 字符以内，全放在窄导航栏也会非常拥挤；所以再次软截断到 12 字符保证视觉密度，完整昵称通过 `title` 属性暴露，用户悬停即可看到全文。

### 通用策略
```javascript
const shortName = fullName.length > 12 ? fullName.slice(0,12) + '…' : fullName;
// 双 tooltip：文字本身 + 整个按钮都带 title，增大命中面积
nameLabel.title = fullName;
iconBtn.title   = fullName + ' · 我的账户';
```

### 各页面实现点

#### 首页导航栏 `updateUserUI()`
- [index.html - updateUserUI 完整逻辑](file:///Users/micahma/Library/Application%20Support/TRAE%20SOLO%20CN/ModularData/ai-agent/work-mode-projects/6a7a8fd2f5e3f47aa813dd7d/13am-site/index.html#L1774-L1804)

```javascript
const fullName  = md.name || state.user.email?.split('@')[0] || 'USER';
const shortName = fullName.length > 12 ? fullName.slice(0,12) + '…' : fullName;
nameLabel.textContent = shortName;
nameLabel.title = fullName;                         // 悬停文字 → 完整昵称
iconBtn.title   = fullName + ' · 我的账户';          // 悬停整个按钮 → 完整昵称 + 提示
```

#### 购物车 / 商品详情页 `renderAuthBadge()`
两处实现完全一致，方便同步维护：
- [cart.html - renderAuthBadge](file:///Users/micahma/Library/Application%20Support/TRAE%20SOLO%20CN/ModularData/ai-agent/work-mode-projects/6a7a8fd2f5e3f47aa813dd7d/13am-site/cart.html#L488-L504)
- [product.html - renderAuthBadge](file:///Users/micahma/Library/Application%20Support/TRAE%20SOLO%20CN/ModularData/ai-agent/work-mode-projects/6a7a8fd2f5e3f47aa813dd7d/13am-site/product.html#L717-L733)

```html
<span class="ab-text" title="完整昵称">截断昵称…</span>
```

---

## 维护备注

1. **调整阈值时必须三处同步**：
   - 写入字符上限 → `saveProfile().slice(0, N)` + `fName.maxlength` + hint 文案
   - 显示软截断 → `index.html` / `cart.html` / `product.html` 三处 `fullName.length > K`
   - CSS 容器宽度 → 与字体/字宽实际情况重新测量，别只改 JS 不改 CSS

2. **移动端再加压时的最后兜底**：
   - 若移动端仍觉得昵称占位过大，可仅在 `@media (max-width: 520px)` 下把 `.user-name-label { display:none }`，让移动端只保留头像，完整昵称放到 `badge.title` 即可

3. **兼容性提醒**：
   - JS `slice` / `.length` 对 4 字节 emoji（🇨🇳 国旗、某些变体）会按 UTF-16 code unit 计数，**视觉 1 个字实际占 2 length 单位**；如有大量 emoji 用户，可把写入/截断逻辑升级为 `[...fullName].slice(0, 12).join('')` 以按码点计算
   - 目前未做此切换，因 20 字符上限足够容纳大多数昵称 + 少量 emoji

4. **为什么三层都要保留**：
   - 只有第 1 层：日后放宽写入上限时所有页面都破版
   - 只有第 2 层：CSS 能防破版但用户看不到全文，也不知道已被截断
   - 只有第 3 层：CSS 一旦被覆盖或容器太小仍会撑开
   - 三层叠加：任意一层失效都能优雅降级，保证至少不会影响整体布局
